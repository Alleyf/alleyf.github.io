# 📦 安装指南

## 🚀 用户安装（推荐）

### 1. 下载插件

从 GitHub Releases 下载 `obsidian-publisher.zip`

### 2. 解压安装

解压到 Obsidian 插件目录：
```
<你的 Vault>/.obsidian/plugins/
```

解压后会自动创建 `obsidian-publisher/` 文件夹

### 3. 启用插件

1. 打开 Obsidian
2. 设置 → 第三方插件
3. 找到 "Multi-Platform Publisher"
4. 点击启用

### 4. 等待服务器启动

插件启用后会自动启动服务器：
- 首次启动会自动安装依赖（需要 1-2 分钟）
- 后续启动只需几秒钟
- 可在设置中关闭自动启动，改为手动控制

### 5. 配置 Cookie

1. 浏览器登录对应平台
2. 按 F12 打开开发者工具
3. Network 标签 → 刷新页面 → 复制 Cookie
4. 在插件设置中粘贴并保存

### 6. 开始使用

打开笔记 → 点击 📤 图标 → 选择平台 → 发布

---

## 🔧 开发者安装

### 从源码构建

```bash
# 1. 克隆项目
git clone <repository-url>
cd obsidian-publisher

# 2. 安装依赖
npm install

# 3. 构建插件
npm run build

# 4. 构建发布包
npm run build:release
```

### 开发模式

```bash
# 启动开发模式（自动重新构建）
npm run dev

# 在另一个终端启动服务器
cd server
npm install
npm start
```

---

## 🐛 常见问题

### Q: 插件启动失败

**原因**: 插件还没有构建

**解决方案**:
```bash
cd /path/to/vault/.obsidian/plugins/obsidian-publisher
npm install
npm run build
```

### Q: Server 无法启动

**检查清单**:
- ✅ 已安装 Node.js 14+
- ✅ 端口 3000 未被占用
- ✅ 网络连接正常

**手动启动**:
```bash
cd server
npm install
npm start
```

### Q: npm install 失败

**解决方案**:
```bash
# 清理缓存
npm cache clean --force

# 使用官方源
npm install --registry=https://registry.npmjs.org/

# 或使用 yarn
npm install -g yarn
yarn install
```

### Q: 依赖安装失败

**解决方案**:
```bash
# 修复 npm 权限
sudo chown -R $USER ~/.npm

# 重新安装
npm install
```

---

## 🔍 验证安装

### 检查文件

确保以下文件存在：
```
obsidian-publisher/
├── main.js          ✅ 必须存在
├── manifest.json    ✅ 必须存在
├── styles.css       ✅ 必须存在
└── server/          ✅ 服务器目录
```

### 检查插件状态

- 设置 → 第三方插件
- "Multi-Platform Publisher" 状态为"已启用"

### 测试服务器

```bash
curl http://localhost:3000/api/health
# 应该返回: {"status":"ok","timestamp":...}
```

### 测试功能

1. 打开任意笔记
2. 按 `Ctrl/Cmd + P` 打开命令面板
3. 输入 "publish"
4. 应该能看到发布相关命令

---

## ✅ 安装成功标志

1. ✅ 插件列表中显示 "Multi-Platform Publisher"
2. ✅ 插件状态为"已启用"
3. ✅ 命令面板中能看到发布命令
4. ✅ 左侧边栏有发布图标 📤
5. ✅ 服务器运行在 http://localhost:3000

**恭喜！你可以开始使用了！** 🎉

---

## 📞 获取帮助

### 查看日志
- Obsidian: `Ctrl/Cmd + Shift + I` 打开开发者工具
- 查看 Console 标签的错误信息

### 提交 Issue
- 包含错误信息
- 包含系统信息（OS、Node.js 版本）
- 包含操作步骤

---

## 🎯 系统要求

- Obsidian 1.0.0+
- Node.js 14+
- 网络连接
- 支持平台：Windows、macOS、Linux

---

**下一步**: 查看 [README.md](./README.md) 了解如何使用
